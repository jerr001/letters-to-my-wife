<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1, user-scalable=0" />
  <title>Edit: <%= letter.title %></title>
  <link rel="stylesheet" href="/css/styles.css" />
</head>
<body>
  <h1>Edit Letter</h1>
  <form action="/letters/<%= letter._id %>?_method=PUT" method="POST">
    <input type="text" name="title" value="<%= letter.title %>" required><br><br>
    <textarea name="body" rows="10" cols="40" required><%= letter.body %></textarea><br><br>
    <button type="submit">Update Letter</button>
  </form>
  <br>
  <a href="/letters/<%= letter._id %>">← Back to Letter</a>
</body>
</html>